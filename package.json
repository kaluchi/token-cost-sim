{
  "name": "token-cost-sim",
  "version": "1.0.0",
  "type": "module",
  "description": "LLM token cost simulator: iterative vs. specialized-tooling approaches",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "cli": "node simulate.js",
    "cli:no-cache": "node simulate.js --no-cache",
    "cli:opus": "node simulate.js --model opus"
  },
  "devDependencies": {
    "chart.js": "^4.4.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
